using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class quat : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("旋转是:" + transform.rotation);

    }

    // Update is called once per frame
    void Update()
    {

    }
}
